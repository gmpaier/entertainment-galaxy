class Fetch {
    async justWatch(input) {
      const myKey = ;
  
      //make request to url
  
      const response = await fetch(
        ``
      );
  
      const data = await response.json();
  
      console.log(data);
  
      return data;